import React, { useState } from 'react';
import { Camera } from './Record';

const App: React.FC = () => {
  return (
    <div className="app-container">
      <Camera />
    </div>
  )
};

export default App;
